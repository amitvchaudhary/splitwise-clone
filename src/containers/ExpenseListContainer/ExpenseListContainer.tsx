import * as React from "react";

type ExpenseListContainerProps = {
  //
};

const ExpenseListContainer: React.FC<any> = () => {
  return <div>ExpenseListContainer</div>;
};

export default ExpenseListContainer;
