import * as React from "react";
import ExpenseSummary from "../../components/ExpenseSummary";

type ExpenseSummaryContainerProps = {
  //
};

const ExpenseSummaryContainer: React.FC<any> = () => {
  return <div>
    <ExpenseSummary />
  </div>;
};

export default ExpenseSummaryContainer;
